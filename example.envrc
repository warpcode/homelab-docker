# Example .envrc file for direnv
# Set the Docker context to use for this homelab environment
# Replace 'remote-docker' with your desired Docker context name if needed
export DOCKER_CONTEXT=remote-docker